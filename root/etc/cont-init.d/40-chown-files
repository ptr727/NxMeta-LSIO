#!/usr/bin/with-contenv bash

# Set directory ownership permissions
# Do not modify /media or /archive ownership it takes too long to start with many files
chown -R ${COMPANY_NAME}:${COMPANY_NAME} \
        /config
